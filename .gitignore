.vercel/*
!.vercel/project.json
.env*.local
.DS_Store
.vercel-cli-cache/
.cache-xdg/
.pw-browsers/
__pycache__/
*.py[cod]
.Python
venv/
.venv/
.npm-cache/
# Local cache for DNR HTML lithology backfill (build_statewide_data.py).
dnr_html_litho_cache.json
dnr_html_litho_cache.json.tmp

# Driller Dashboard local smoke tests (optional)
_driller_sample_viewer/
_driller_sample_out.jsonl.gz

# Drill Hub is a separate repo — do not nest it here (avoids accidental commits / merged trees).
well-driller-dash-board/
well-driller-dashboard/
apps/hub/

# Full export: raw CSV is ~106 MB (over GitHub's 100 MB file cap). The canonical copy in git is
# dnr_wells_full.csv.gz — same rows as dnr_wells_full.csv after decompressing:
#   gzip -dk dnr_wells_full.csv.gz
# Keep the uncompressed file local only so it is not committed by mistake.
dnr_wells_full.csv
dnr_wells_slim.csv
dnr_wells_slim.csv.gz
