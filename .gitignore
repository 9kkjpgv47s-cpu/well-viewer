.vercel/*
!.vercel/project.json
.env*.local
.DS_Store
__pycache__/
*.py[cod]
.Python
venv/
.venv/
# Local cache for DNR HTML lithology backfill (build_statewide_data.py).
dnr_html_litho_cache.json
dnr_html_litho_cache.json.tmp

# Driller Dashboard local smoke tests (optional)
_driller_sample_viewer/
_driller_sample_out.jsonl.gz

# Drill Hub is a separate repo — do not nest it here (avoids accidental commits / merged trees).
well-driller-dash-board/
well-driller-dashboard/
apps/hub/

# Large source CSV stays local; only the 2 gzipped chunks are committed and deployed.
dnr_wells_full.csv
dnr_wells_slim.csv
dnr_wells_slim.csv.gz
